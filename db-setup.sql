-- CREATE TABLE kitbricks
-- (
--   id INT AUTO_INCREMENT,
--   kitId INT,
--   brickId INT,

--   PRIMARY KEY (id),

--   FOREIGN KEY (kitId) 
--     REFERENCES kits (id) 
--     ON DELETE CASCADE,

--   FOREIGN KEY (brickId) 
--     REFERENCES bricks (id)
--     ON DELETE CASCADE
-- );

-- TRUNCATE TABLE kitbricks;